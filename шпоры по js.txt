js
console.log('Привет от JavaScript!'); //вывод сообщения в консоль
console.log(document.querySelector('.page')); //найти и вывести элемент по классу в консоль
document.querySelector('.название элемента').classList.remove('название класса'); //удаление класса у элемента
document.querySelector('.название элемента').classList.add('названия класса'); //присвоение класса к элементы без изменения исходного кода
let название переменной = 'Я значение переменной!'; //создание переменной. в переменные можно сохранять и элементы let header = document.querySelector('header');
let button = document.querySelector('button');
button.onclick = function() { //обработчик событий
  console.log('Клик!'); //вывод текста в консоль
}; //отображение сообщение в консоль при нажатие на кнопку
элемент.classList.toggle('класс который должен появляться обратно'); //чередование классов
let paragraph = document.querySelector('p');
paragraph.textContent = 'Здесь был Кекс. Мяу!';//изменит текст в тэги <p> без изменения исходного кода
message.textContent = 'Какой <strong>непонятливый</strong> браузер!'; //заменит message.textContent на другое значение.
let someForm = document.querySelector('.some-form'); //если форма будет отправлена в консоль выведится форма отправлена
someForm.onsubmit = function() {
  console.log('Форма отправлена!');
};
// однострочные коментарии
/*
И многострочные.
Они могут отключить сразу несколько строк кода.
*/
let input = document.querySelector('input'); // вывести в консоль написанное в value
console.log(input.value);
// Выведет: Кекс
paragraph.textContent = input.value // вывести данные из value